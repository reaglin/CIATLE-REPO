AUTOMATED (PowerShell - preferred on this machine)
From PowerShell in the CIATLE-REPO root directory:
  .\Deployment\deploy-update.ps1

Uses native Windows OpenSSH (ssh/scp) and the dotnet CLI. You will be
prompted for the server password up to three times unless key auth is set up.
Add -SkipMigrations when the release contains no new EF migration.

AUTOMATED (bash)
Note: bash is NOT on PATH on this machine. Git Bash is installed at
  C:\Program Files\Git\bin\bash.exe
so the bash script must be invoked by full path:
  & "C:\Program Files\Git\bin\bash.exe" Deployment/deploy-update.sh
Prefer the PowerShell script above - Git Bash rewrites /tmp/presemaker-release
into a Windows path when passing it to dotnet, which is a needless risk in a
production deploy.


MANUAL:
From PowerShell to Open Server
>ssh root@129.121.101.162
>(Enter password)

DIRECTIONS 

PowerShell
  dotnet publish PreseMakerRepo.Api -c Release -r linux-x64 --self-contained false -o /tmp/presemaker-release

Server:
systemctl stop presemaker-repo 
rm -f /opt/presemaker-repo/app/PreseMakerRepo.Api
systemctl is-active presemaker-repo

PowerShell:
  scp -r /tmp/presemaker-release/. root@129.121.101.162:/opt/presemaker-repo/app/

Server:
  chmod +x /opt/presemaker-repo/app/PreseMakerRepo.Api 
  chown -R presemaker:presemaker /opt/presemaker-repo/app  
  systemctl start presemaker-repo
  systemctl status presemaker-repo --no-pager
